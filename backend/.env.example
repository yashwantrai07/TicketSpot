MONGO_URI=mongodb://127.0.0.1:27017/ticketspot
PORT=5000
JWT_SECRET=replace_with_strong_secret
CORS_ORIGIN=http://localhost:5173

# Optional — if unset, OTP emails are logged to the server console
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=TicketSpot <noreply@localhost>
